# Architecture Docs

这里存放模块级架构说明和长期约束。

根目录 `ARCHITECTURE.md` 是短入口，只描述系统地图和硬边界；更深的模块说明放在本目录。

产品行为约定在 `ARCHITECTURE.md` 链接的项目产品文档中维护；采用 OpenSpec 时以 `openspec/specs/` 为规格来源。本目录只解释结构、依赖方向、模块边界和设计取舍，不复制需求规范。
