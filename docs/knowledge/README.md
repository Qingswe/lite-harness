# Knowledge Archive

日常协作只沉淀有复用价值的结论，可引用提交、PR 或测试证据，不要求每个任务生成摘要。以下 OpenSpec 归档引用用于自动循环。

这里存放完成变更后的长期知识，不存放当前任务状态。

推荐结构：

```text
docs/knowledge/
├── changes/
└── pitfalls/
```

- `changes/`：已完成变更的短摘要和索引。
- `pitfalls/`：已踩坑、异常模式、Unity 特有问题。

变更仍以 `openspec/changes/archive/` 为归档事实来源；这里是便于检索的知识层。
